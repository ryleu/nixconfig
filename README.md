# nixconfig

NixOS configuration for my machines.

## Contents

- `./configuration.nix` holds stuff that still needs to be moved over
- `./modules` contains the rest of the modules
- `./modules/hardware` contains per-host, hardware-specific configuration
- `./modules/home` contains home manager modules
- `./modules/system` contains nixos modules

